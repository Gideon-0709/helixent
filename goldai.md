# Gold AI Design

## 1. 设计范围

本文档用于描述 Gold AI 的设计范围和系统边界。Gold AI 是面向 ERP 系统的后台 AI 服务，它不直接承载 ERP 主系统的业务页面、数据存储和核心业务流程，而是通过 API 的方式向主系统提供 AI 能力。

Gold AI 对外暴露一组服务接口，主系统可以通过这些接口创建和管理 Agent、向 Agent 输入内容、获取 Agent 的返回结果，以及查询 Agent 的执行过程和状态。主系统负责在产品界面中承载用户交互，Gold AI 负责在后台完成理解、推理、工具调用、内容生成和任务编排。

Gold AI 同时也会通过 API 调用主系统和数据库侧提供的能力。例如，AI 可以查询 ERP 指标、读取当前页面上下文、获取组织和人员信息、生成任务草稿、保存经营简报、归档会议复盘等。这些能力不由 AI 服务直接实现业务数据逻辑，而是通过主系统提供的稳定接口完成。AI 服务的职责是选择合适的工具、组织调用顺序，并将结果转化为可解释、可执行、可追踪的业务输出。

Gold AI 是一个多 Agent 系统。不同 Agent 可以拥有不同的角色、目标、管理范围、可用工具、技能流程和权限边界。例如，总经理助理、区域经理、店长等 Agent 可以基于同一套底层框架运行，但根据各自职责关注不同数据、生成不同视角的分析结论，并参与不同层级的任务协作。

单个 Agent 的运行采用类 ReAct 的框架，即通过“思考、调用工具、观察结果、继续推理”的循环完成任务。Agent 不只是一次性生成文本，而是可以在多轮推理中根据问题选择工具、调用主系统 API、读取返回结果，再继续分析并给出最终输出。多 Agent 协作、经营简报生成、任务分配和会商等更复杂的业务流程，则可以在 ReAct Agent 之外通过 workflow 进行编排。

## 2. Agent 开发模型

Gold AI 的核心开发对象是单个 Agent。每个 Agent 都基于同一套类 ReAct 的运行框架，但会根据自身角色配置不同的 prompt、workflow、skill 和 tools。框架本身负责通用的运行循环，包括接收输入、调用大模型、识别工具调用、执行工具、读取工具结果，并继续下一轮推理。具体业务能力则通过角色化配置和工具扩展实现。

单个 Agent 的开发主要包含四类内容：

1. **Prompt**：定义 Agent 的身份、职责、边界和输出风格。例如，总经理助理应关注公司整体经营、风险提示和任务协调；区域经理应关注区域经营情况、门店对比和区域问题；店长应关注单店经营、人员执行和日常任务落地。

2. **Workflow**：定义某类业务任务的阶段性流程。例如，经营简报可以包括选择时间窗口、获取关键指标、分析异常、生成建议、保存简报等步骤。Workflow 用于组织一组确定性的业务阶段，而不是替代 Agent 的推理能力。

3. **Skill**：定义 Agent 在特定任务下应遵循的专业方法。例如，经营分析 skill 可以要求 Agent 先确认指标口径，再比较环比、同比和目标完成率，最后输出风险和行动建议。Skill 更像是 Agent 的工作手册，帮助 Agent 在特定场景下稳定地完成任务。

4. **Tools**：定义 Agent 可以实际调用的外部能力。Tools 通常封装主系统或数据库提供的 API，例如查询 ERP 指标、读取当前页面上下文、获取组织架构、创建任务草稿、保存简报、写入知识库等。Agent 只能通过 tools 获取和修改外部系统中的数据。

通过这种方式，不同 Agent 可以共享同一套底层 ReAct loop，但拥有不同的业务能力组合。多 Agent 系统并不是为每个角色重写一套框架，而是在同一个框架上注册不同的 Agent profile。每个 profile 包含自己的名称、角色、prompt、可用 skill、可用 tools、权限范围和默认工作方式。

例如：

- 总经理助理 Agent 负责全局视角的问题理解、公司经营总览、任务协调和多 Agent 会商建议。
- 区域经理 Agent 负责区域经营分析、区域风险识别、门店对比和区域任务建议。
- 店长 Agent 负责单店经营分析、门店执行反馈、人员任务落实和本店问题复盘。

多 Agent 协作的控制逻辑不在 Gold AI 服务中完整实现。也就是说，Gold AI 不负责主系统中的会议室 UI、任务流转、审批流、通知、业务状态机和最终业务闭环。这些流程属于主系统的产品和业务工作流，应由主系统开发和管理。

Gold AI 的职责是为主系统提供多个 Agent 的底层能力。主系统可以根据自己的业务流程决定何时调用哪个 Agent、如何展示 Agent 的回复、是否将某个 Agent 的输出转化为任务、以及如何组织多个 Agent 的结果。Gold AI 则负责确保每个 Agent 能够稳定地理解输入、调用工具、生成结构化结果，并返回可供主系统继续处理的数据。

因此，Gold AI 与主系统之间的关系可以理解为：

```text
主系统
  -> 负责产品界面、业务流程、任务状态、权限审批和用户操作

Gold AI
  -> 负责 Agent 能力、推理过程、工具调用、内容生成和结构化输出

主系统 API / 数据库 API
  -> 为 Gold AI 的 tools 提供数据和动作能力
```

这种设计可以让 AI 服务保持相对独立和通用，同时又能通过不同 Agent profile 支持主系统中的多种 AI 交互场景。后续如果需要新增角色，例如财务分析员、运营分析员或客服主管，也可以通过新增对应的 prompt、skill、workflow 和 tools 组合来扩展，而不需要重写底层 Agent 框架。

## 3. 测试平台与 Debug Panel

为了支持 Gold AI 的持续开发、调试和业务验证，需要构建一个面向开发和实验的 Debug Panel。Debug Panel 是 Gold AI 的测试平台，它不面向最终业务用户，而是用于可视化观察 Agent 的运行过程、调试 prompt、验证 skill 和 workflow，并帮助团队在正式接入主系统之前完成 AI 能力实验。

实现 Debug Panel 的主要原因有三点。

第一，AI Agent 的运行过程并不是一次简单的函数调用。单个 Agent 在类 ReAct 框架下会经历多轮推理、工具选择、工具调用、观察结果和继续分析。如果缺少可视化工具，开发者只能通过日志或最终输出判断问题，很难知道 Agent 在哪一步理解错了、选错了工具、拿到了错误数据，或是在生成结论时遗漏了关键信息。Debug Panel 可以把这些过程展开为可观察的事件流，让开发者清楚看到一次 Agent run 的完整链路。

第二，Gold AI 后续会包含大量 prompt、skill 和 workflow 的设计工作。这些内容并不完全由工程师独立完成，很多需要业务人员、运营人员或管理人员共同参与。例如经营简报的结构、风险提示的口径、任务分配建议的表达方式，都需要从业人员基于业务经验反复调整。业务人员通常不具备代码调试能力，也不应该被要求通过修改源码来参与实验。Debug Panel 可以提供一个低门槛的实验平台，让他们能够在界面中查看、编辑和验证 prompt、skill 等内容，并直接观察 Agent 输出效果。

第三，Debug Panel 可以作为 Gold AI 与主系统正式集成前的中间验证环境。主系统接口尚未完全稳定时，可以通过 mock tools 或测试 API 先验证 Agent 逻辑；主系统接口稳定后，也可以通过 Debug Panel 验证真实 API 调用、数据返回、权限边界和输出质量。这样可以降低直接接入主系统的风险，避免 AI 能力在不可观察的状态下进入业务流程。

Debug Panel 应包含以下核心能力。

1. **Agent 列表与角色选择**

   Debug Panel 左侧展示当前可测试的 Agent，例如 GMA、RM、SM 等。测试人员可以选择不同 Agent 类型创建会话，并在同一界面中对比不同角色的行为差异。每个 Agent 会话应保留自己的上下文、运行记录和执行状态，便于观察同一 Agent 在连续对话中的表现。

2. **Agent 输入与结果观察**

   Debug Panel 提供 Agent 输入区，测试人员可以直接向选定 Agent 输入问题或任务。系统会展示 Agent 的最终回答，同时保留本次运行的 runId、sessionId 和上下文关系。这样可以模拟主系统调用 Gold AI 的过程，而不依赖主系统前端页面。

3. **运行事件时间线**

   Debug Panel 应可视化展示一次 Agent run 的关键事件，包括 run started、prompt loaded、skills loaded、tools registered、model started、assistant message、tool started、tool completed、tool result、token usage、final answer、run completed 或 run failed。通过事件时间线，开发者可以定位 Agent 是在哪一步出现偏差。

4. **Prompt / Skill / Tool 资源查看与编辑**

   Debug Panel 应提供资源编辑能力，用于查看和调整系统 prompt、skill 定义和 tool 配置。测试人员可以在界面中修改 prompt 或 skill 内容，并立即通过 Agent 输入区验证效果。对于 tool，可以展示其名称、描述和输入参数结构，帮助业务人员理解 Agent 能调用哪些能力。

5. **多轮上下文调试**

   Debug Panel 应支持同一个 Agent 会话中的多轮输入。这样可以验证 Agent 是否正确保留上下文、是否能够基于前一次工具结果继续推理，以及在连续问题中是否保持稳定的角色边界。对于需要跟进分析、任务拆解或会商前置沟通的场景，多轮上下文调试非常重要。

6. **工具调用观测**

   Debug Panel 应清晰展示 Agent 何时选择了哪个 tool、传入了什么参数、工具返回了什么结果，以及这些结果如何进入下一轮推理。对于 Gold AI 来说，大量能力都依赖主系统 API 和数据库 API，工具调用观测是判断 AI 是否正确使用业务接口的关键。

7. **Token 与成本观察**

   Debug Panel 应展示每次模型调用的 token 使用情况，包括 prompt tokens、completion tokens 和 total tokens。后续在经营简报、多 Agent 会商等高成本场景中，token 统计可以帮助团队评估不同 workflow 和 prompt 设计的成本。

8. **会话归档与复盘**

   Debug Panel 应支持将一次或多次 Agent 会话归档，保存为可回溯的调试记录。归档内容可以包括输入、输出、关键事件、工具调用、错误信息和最终结果。这样可以支持问题复盘、prompt 版本对比、业务评审和知识沉淀。

9. **错误定位与失败分析**

   当 Agent run 失败时，Debug Panel 应展示失败事件、错误信息、失败位置和相关上下文。常见失败包括模型调用失败、工具调用失败、主系统 API 返回错误、权限不足、参数不合法、结果格式不符合预期等。Debug Panel 的目标不是隐藏失败，而是让失败可定位、可解释、可修复。

10. **主系统集成前的实验环境**

    Debug Panel 可以独立于主系统运行，作为 Gold AI 的开发和实验入口。工程团队可以先在 Debug Panel 中验证 Agent、prompt、skill、workflow 和 tools 的组合是否合理，再将稳定能力通过 API 暴露给主系统使用。

综上，Debug Panel 是 Gold AI 工程体系中的重要组成部分。它不是最终产品界面，而是 AI 能力开发、调试、业务协同和质量验证的基础设施。通过 Debug Panel，团队可以把原本黑盒的 Agent 运行过程变成可观察、可调整、可复盘的工程过程，从而更安全地推进多 Agent 能力在 ERP 主系统中的落地。

## 4. 需求总览与优先级评估

根据当前需求，Gold AI 需要支持的能力可以分为六类：AI 经营简报、AI 总助浮窗、任务总结与分配、多 Agent 会商、任务下发与执行跟踪、会议与任务复盘及知识累积。这些需求之间存在明显的依赖关系，不能简单并行推进。整体上应优先建设基础数据能力、单 Agent 能力和任务草稿能力，再逐步推进多 Agent 协作和业务闭环。

从工程角度看，这些需求可以分为三类。

第一类是 **主要依赖 tools 和 prompt/skill 的能力**。这类能力通常不要求 Gold AI 实现复杂业务状态，只需要通过 tools 调用主系统 API，获取数据后由 Agent 生成结构化内容。例如经营简报、即时问答、文档总结、任务草稿生成等。这类需求工程难度相对较低，适合作为第一阶段优先开发。

第二类是 **需要 workflow 编排的能力**。这类能力涉及多个步骤，例如先收集信息，再分析，再生成建议，再保存结果。它们不一定需要复杂的多 Agent 协作，但需要 Gold AI 在 ReAct Agent 外增加一层轻量 workflow，以保证步骤顺序、输入输出结构和失败处理。例如公司经营总览、任务总结与分配、会议复盘归档等。

第三类是 **需要主系统深度配合的能力**。这类能力涉及业务状态流转、用户确认、权限控制、通知提醒、任务生命周期和多角色协同。Gold AI 可以提供分析、建议和结构化结果，但最终业务闭环必须由主系统实现。例如任务下发、执行跟踪、多 Agent 会商 UI、任务反馈和归档等。这类需求优先级可以高，但工程落地必须依赖主系统接口和产品流程。

### 4.1 需求难度与优先级总览

| 需求 | 主要价值 | 工程难度 | 建议推进优先级 | 主要依赖 |
| --- | --- | --- | --- | --- |
| AI 经营简报 | 快速体现 AI 对 ERP 数据的分析价值，为不同角色生成经营洞察 | 中 | 高 | ERP 指标 API、Agent prompt/skill、简报保存 API |
| AI 总助浮窗 | 提供统一 AI 入口，支持即时问答和任务识别 | 中 | 高 | 当前页面上下文 API、指标查询 API、任务草稿 API |
| 任务总结/分配 | 将问题、风险和待办转化为可执行任务建议 | 中到高 | 中 | 任务 API、组织人员 API、职责范围 API |
| 会议/任务复盘和知识累积 | 沉淀会议结论、任务结果和业务知识 | 中 | 中 | 归档 API、知识库 API、任务反馈 API |
| 任务下发及执行跟踪 | 将 AI 建议转化为主系统中的真实任务闭环 | 高 | 低 | 主系统任务流、通知、状态流转、权限与审批 |
| Multi-Agent 会商系统 | 支持多个角色共同分析问题，形成综合方案 | 高 | 低 | 多 Agent 编排、会商 UI、任务生成、人工确认 |

这里的优先级不是业务价值的绝对排序，也不等同于整个项目的需求优先级，而是从 Gold AI 工程可实施性、依赖成熟度和阶段性验证价值出发给出的推进建议。高优先级需求应优先形成可演示、可验证的闭环；中优先级需求适合在基础能力稳定后推进；低优先级需求并不代表业务价值低，而是说明它们工程复杂度更高、主系统依赖更强，不建议第一阶段完整实现。

### 4.2 AI 经营简报

AI 经营简报是最适合作为第一阶段核心能力的需求。它的业务价值清晰，输出结果容易被评估，也能够直接验证 Gold AI 对 ERP 数据的理解和分析能力。

经营简报分为两类：

1. **角色经营简报**：不同 Agent 根据自己的管理范围生成经营简报。例如店长关注单店经营，区域经理关注区域内门店表现，总经理助理关注公司整体经营和跨区域风险。

2. **公司经营总览**：总经理助理汇总各 Agent 的经营简报，提炼关键风险、核心变化和需要管理层关注的问题，最终形成公司级经营总览。

该需求工程难度为中等。难点不在文本生成，而在指标口径和数据接口。ERP 指标必须由主系统或数据库 API 提供统一口径，Gold AI 不应自行猜测或临时拼接口径。建议第一阶段先实现固定模板的经营简报，例如核心指标、异常变化、重点板块、风险提示、建议动作和待跟进问题。

### 4.3 AI 总助浮窗

AI 总助浮窗是主系统中的统一 AI 入口，适合承接老板在浏览 ERP 数据时的即时问题和初步任务诉求。该需求的前端形态由主系统负责，Gold AI 负责提供后台问答和任务识别能力。

AI 总助浮窗主要包含两类能力：

1. **即时问答**：根据当前页面上下文和主系统数据 API 回答问题。例如用户看到总销售额后询问某个品类的销售额，Gold AI 应调用当前页面上下文 API 和指标拆解 API，而不是仅依赖语言模型猜测。

2. **深度沟通与任务识别**：当老板提出需要进一步分析的问题或待办事项时，AI 总助可以识别其中的问题、风险和任务意图，并生成任务草稿，递交给任务总结/分配窗口。

该需求工程难度为中等。它需要主系统提供当前页面上下文、用户身份、数据过滤条件和相关指标查询接口。第一阶段建议先支持问答和任务草稿生成，不直接自动创建正式任务。

### 4.4 任务总结与分配

任务总结与分配用于收集来自 AI 总助、经营简报风险提示和会商结果中的待处理事项，并将其整理成可执行任务建议。它需要解决的问题包括任务去重、任务归类、负责人建议和执行方式建议。

该需求工程难度为中到高。原因是任务分配不能完全依赖语言模型判断，还需要主系统提供组织架构、人员角色、区域/门店归属、职责范围和任务历史等信息。Gold AI 可以生成建议，但是否下发、下发给谁、是否需要审批，应由主系统控制。

第一阶段建议将其实现为“任务草稿中心”，由 Gold AI 输出结构化任务建议：

```text
任务标题
任务背景
建议负责人
建议完成时间
验收标准
关联问题或风险
来源 Agent / 来源简报 / 来源会商
```

正式任务创建应在老板或主系统确认后执行。

### 4.5 Multi-Agent 会商系统

Multi-Agent 会商系统的目标是让多个 Agent 针对老板提出的问题发表各自观点，由 AI 总助汇总会议要点、给出建议，并在必要时形成任务分配方案。它是业务价值较高但工程复杂度也最高的需求之一。

完整会商系统涉及问题分析、参与 Agent 选择、多轮发言、老板追问、冲突观点总结、最终方案生成和任务拆解。如果第一阶段直接实现实时多轮会商，会带来较高的工程复杂度，包括会商状态管理、流式输出、多 Agent 并发、发言顺序控制、人工打断和成本控制。

建议采用替代方案：第一版先实现 **异步多 Agent 意见汇总**。流程可以是：

```text
老板提出问题
AI 总助分析问题并选择参与 Agent
各 Agent 独立生成观点和建议
AI 总助汇总共识、分歧、风险和行动建议
老板确认后进入任务草稿或任务下发流程
```

这种方案可以保留多 Agent 的业务价值，同时显著降低实时会商系统的工程风险。

### 4.6 任务下发及执行跟踪

任务下发及执行跟踪是把 AI 建议转化为真实业务闭环的关键能力。AI 总助可以根据会商结果或任务分配建议生成任务，并通过主系统 API 下发给对应负责人。执行过程中，主系统负责提醒、状态流转和反馈收集，Gold AI 负责解释任务背景、整理执行反馈、总结结果和判断风险。

该需求工程难度较高，主要因为它深度依赖主系统的任务模块。任务状态、通知、审批、执行人反馈和最终归档都应该由主系统承载。Gold AI 不应另建一套任务系统，否则会造成状态割裂和权限风险。

建议在第一阶段只输出任务草稿和任务说明；第二阶段再接入主系统任务创建 API；第三阶段再支持执行反馈总结、逾期风险提示和任务复盘归档。

### 4.7 会议/任务复盘和知识累积

会议/任务复盘和知识累积用于沉淀每次会议、任务执行和业务决策产生的结果。它可以帮助 Gold AI 后续在相似问题中引用历史经验，也能为主系统形成可检索的管理知识库。

该需求工程难度为中等。它需要主系统提供归档和知识库 API，但 AI 侧的能力相对明确：总结会议结论、提取决策、归纳任务结果、生成复盘文档和沉淀知识条目。

需要注意的是，知识累积不能无限制保存所有 AI 输出，否则会很快产生噪音。第一阶段应只沉淀三类内容：

```text
明确决策
经过验证的业务规则
任务复盘结论
```

后续再考虑更复杂的知识检索、版本管理和引用机制。

### 4.8 推荐实施路线

建议按照三个阶段推进。

**第一阶段：验证 AI 基础价值**

优先实现 AI 经营简报、AI 总助即时问答和任务草稿生成。这一阶段重点验证 Agent 能否通过 tools 正确调用主系统 API，并产出稳定的结构化结果。

**第二阶段：形成协作闭环雏形**

推进任务总结/分配、会议/任务复盘和知识累积。该阶段开始引入轻量 workflow，让 AI 输出能够进入主系统的任务和知识体系，但仍保留人工确认。

**第三阶段：推进多 Agent 协作和业务闭环**

在前两阶段稳定后，再推进 Multi-Agent 会商、任务下发和执行跟踪。该阶段需要主系统提供更完整的任务流、权限、通知和会商 UI，Gold AI 负责多 Agent 分析、建议生成和结构化输出。

整体策略是先做数据分析和内容生成，再做任务草稿和协作建议，最后再做自动下发和多 Agent 会商闭环。这样可以在较低工程风险下尽快验证 Gold AI 的核心价值。
